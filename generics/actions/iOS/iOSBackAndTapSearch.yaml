appId: ${APP_ID}
---
# Verificar e interagir com o primeiro botão
- waitForAnimationToEnd
- assertVisible:
    id: ${output.plpScreen.backButton}
- tapOn: 
    id: ${output.plpScreen.backButton}

# Verificar e interagir com o segundo botão
- waitForAnimationToEnd
- assertVisible:
    id: ${output.searchScreen.backButton}
- tapOn: 
    id: ${output.searchScreen.backButton}